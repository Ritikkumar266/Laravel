
To create a new controller:  "php artisan make:controller controller_name"
To create a new Model: "php artisan make:model model_name:


//Passing data (variable, integer, array) to views:
//1. associative array
//2. with chaining
//3. compact
//4. with variable

//Attaching headers to the route and on landing page

//controller
//Basic controller
//Returning string from controller
//Returing view from controller
//Passing data to views from controller
//Returning json from controller

//Form handling regex
// /^ --start of string
// $/ --end of string
//[alowed characters]
// [A-Z]
// [a-z]
// [0-9]
// [!@#$%&^*]
// \s --space
// \t --tab
// \d --[0-9]
// + --allowing multiple characters like aman ,nitin


Master file is linked with routing file web.php placed in route folder to make it SPA in MVC architecture




Localization ->Multi language
 php artisan lang:publish

 role of env file -> For localization , SMTP port , environment configuration for database

Eloquent ORM (Object relational Method) - mapping
 MYSQL (defualt)- username- root , pwd- null


 Model---------Controller ----------Views
{model will perform operations in controller and controller will do the operations in views(like passing data)}

To make view interactive like DB data we can use Tailwind

php artisan migrate 
php artisan session:table
php artisan cache:table 


-> creates a migration file for a table named cache
 


 to check the mongodb version ->  mongod --version


 php -i | findstr "PHP Version" ->command (8.2.12)

 php -i | findstr "Thread"  ->command(enabled, Windows Threads)

 php -i | findstr "Architecture" -> command (x64)

 composer require mongodb/laravel-mongodb


 php artisan make:controller GrocceryShop -m Item